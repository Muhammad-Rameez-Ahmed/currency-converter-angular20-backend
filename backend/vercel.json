{
  "version": 2,
  "builds": [
    {
      "src": "api/index.js",
      "use": "@vercel/node",
      "config": { 
        "includeFiles": ["dist/**"]  
      }
    }
  ],
  "buildCommand": "npm install && npm run build"
}